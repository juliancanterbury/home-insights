# Home Insights — literal gas meter update

Replace `index.html` and copy the `assets` folder into the repository root.

The gas meter illustration uses dynamic HTML overlays for the five number wheels and two decimal dial needles. Confirming a photo-derived reading updates the visible meter immediately. Google Sheets persistence remains the next stage.
